name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # 1. JOB: Baue alle Binaries parallel
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
            executable: AiNetLinter.exe
          - os: ubuntu-latest
            rid: linux-x64
            executable: AiNetLinter
          - os: macos-latest
            rid: osx-x64
            executable: AiNetLinter-intel
          - os: macos-latest
            rid: osx-arm64
            executable: AiNetLinter-arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish Single File
        run: >
          dotnet publish src/AiNetLinter/AiNetLinter.csproj 
          -c Release 
          -r ${{ matrix.rid }} 
          --self-contained true 
          -p:PublishSingleFile=true 
          -p:PublishTrimmed=false 
          -o ./publish

      - name: Rename and Prepare
        shell: pwsh
        run: |
          $publishedExecutable = Get-ChildItem -Path "./publish" -File | Where-Object { $_.Name -match "AiNetLinter" -and $_.Extension -notmatch "json|pdb" }
          $newName = "${{ matrix.executable }}"

          if ($publishedExecutable) {
            Rename-Item -Path $publishedExecutable.FullName -NewName $newName
          } else {
            Write-Error "Keine ausfuehrbare Datei gefunden!"
            exit 1
          }

      - name: Upload Artifacts to Action
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.rid }}
          path: |
            ./publish/${{ matrix.executable }}
          if-no-files-found: warn
          retention-days: 1 # Brauchen wir nur kurz fuer den naechsten Job

  # 2. JOB: Erstelle das GitHub Release (Laeuft nur EINMAL, wenn Build fertig ist)
  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: binaries-*
          path: ./release-files
          merge-multiple: true # Legt alle Dateien (exe, mac, linux) in denselben Ordner

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ./release-files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

---
status: active  # active | done
task: flaky-and-test-performance
derived_from: konzept.md
created_at: 2026-08-07T08:58:29+02:00
last_updated: 2026-08-08T09:15:00+02:00
created_by_model: MiniMax-M3
created_by_model_knowledge_cutoff: 2026-01
---

# Roadmap: flaky-and-test-performance

Grober Anker, kein Detailplan — Detail-Steps entstehen erst JIT im
Step-Modus des Planers, siehe `../spec.md` §7.2. Diese Datei wird
laufend angepasst (Epics abgehakt, ergänzt, umformuliert oder als
obsolet markiert) — kein starres Vorab-Dokument.

## Tech-Stack-Notiz

Aus dem Projekt abgeleitet (Konzept, AGENTS.md, AiNetLinterRichtlinien.mdc,
Test-Projekt-Setup), einmalig hier (nicht pro Step neu):

- **Build-Command:** `dotnet build` (Solution-Root; `TreatWarningsAsErrors=true` in beiden Projekten)
- **Test-Command:** `dotnet test` (voller Lauf) — loggt nach `TestResults/latest.trx` via `.runsettings`; xUnit-v3-Konfig in `src/AiNetLinter.Tests/xunit.runner.json` (`parallelizeTestCollections=true`, `longRunningTestSeconds=3`)
- **Fast-Path-Test-Command:** `dotnet test --filter Category=Unit` (oder `Category!=Integration`) — wird in EPIC-04 etabliert/verifiziert
- **Lint-Command:** `dotnet run --project src/AiNetLinter -- --self-lint` (Self-Lint, muss `OK` bleiben)
- **Sprache / Tooling:** .NET 10 (`net10.0`), xUnit v3 (`xunit.v3.core`/`xunit.v3.assert` 3.2.2), PowerShell 7, Windows-only
- **Code-Style-Kurzfassung** (aus `AiNetLinter.mdc` + `AiNetLinterRichtlinien.mdc`):
  - `sealed` für konkrete Klassen; Methoden ≤ 60 Zeilen; ab 5 Parametern `record` als Input-Object; `#nullable enable` am Dateianfang
  - Kein leeres `catch`, kein `dynamic`, `out` nur in `Try*`-Methoden, `async void` verboten (außer Event-Handler)
  - Result-Pattern bevorzugt statt Exceptions für Fehlerfälle
  - Tests-Projekt: `MaxMethodLineCount` 100, `EnforceSealedClasses` aus (`*.Tests`-Override)
  - Architektur: **kein** `AssemblyLoadContext`, **kein** DI-Container, monolithisches CLI bleibt schlank
  - **Sparsame Kommentare** — Self-Documenting Code bevorzugen; **keine** Verweise auf `step-NNN`/`TD-XXX`/`EPIC-XX`/Ticket-IDs in Code-Kommentaren; **keine** Refactoring-Historie; XML-Doc nur bei unkonventionellem *Why*
  - Zero-Warning-Direktive: kein Commit mit rotem Build
- **Commit-Konventionen:** Conventional Commits **auf Deutsch**, imperativ (z. B. `feat:`, `fix:`, `refactor:`), Subject ≤ 72 Zeichen, Body mit `### Commit-Vorschlag`-Block am Ende der Agent-Antwort (Pflicht — siehe AiNetLinterRichtlinien.mdc §4). Für diesen Task zusätzlich mit Suffix `[flaky-and-test-performance]` im Subject.
- **Test-Kategorien (xUnit v3):** `[Trait("Category", "Unit")]` / `"Integration"` (siehe EPIC-02 — aktuell nur 86 von ~1087 Tests getraggt).

## Regel-Index

Ein Eintrag pro Datei in `<rules_dir>/**` — Kurzbeschreibung, kein Volltext.
Zweck: Der Step-Modus-Planer ist pro Aufruf eine frische, isolierte Session
ohne Erinnerung an diesen Roadmap-Modus-Aufruf — er kann `<rules_dir>/**`
nicht bei jedem Step neu komplett lesen (Kosten), liest aber diesen Index
(steht hier in `roadmap.md`) und dann gezielt nur die 1-2 Dateien, die zum
aktuellen Step passen.

- `.agents/rules/AiNetLinter.mdc` — Auto-generierte C#-Codequalitätsregeln aus `rules.json` (sealed, LOC/Cyclomatic/Cognitive-Limits, agent-resilience, Architektur-Verbote wie Phantom-Dependencies, `*.Tests`-Overrides); für jeden Code-Step prüfen.
- `.agents/rules/AiNetLinterRichtlinien.mdc` — Manuelle Architektur- und Workflow-Leitlinien: monolithisches CLI ohne ALC/DI, Windows/PowerShell-Tooling, xUnit-v3-Parallelität, Self-Lint-Pflicht, Zero-Warning-Direktive, `### Commit-Vorschlag`-Block-Pflicht, sparsame Kommentare ohne Step-/TD-/EPIC-IDs; **besonders relevant** für fast jeden Step in diesem Task (Test-/Fixture-Änderungen berühren §4, §5, §6).

## Epics

- [x] EPIC-01: Spike — Fixture-Sharing validieren (Vorarbeit) — `SymbolGraphMcpFixture` probeweise auf `ICollectionFixture` umgestellt, Vorher/Nachher gemessen. **Spike-Befund:** Sharing bringt keinen Performance-Gewinn (isoliert +5,3 %, voll +8,1 %); Sequenzialisierung innerhalb der Collection überkompensiert die Subprozess-Start-Einsparung. Empfehlung: EPIC-05 (mockbarer Lade-Pfad im Produktionscode) vermutlich nötig; finale Entscheidung in EPIC-03. → `step-001` (approved, Commit `bf5de7e`).

- [ ] EPIC-02: Category-Traits flächendeckend nachziehen — alle ~1000 ungetraggten Testmethoden mit `[Trait("Category", ...)]` versehen (Taxonomie: `Unit` vs. `Integration`/`Subprocess`/`Live`; genaue Aufteilung JIT in EPIC-01, falls Sharing Tests in andere Klassen verschiebt). Voraussetzung für EPIC-04 (Fast-Path per `--filter`). Kann parallel zu EPIC-01 begonnen werden, Konzept §"Wie" Schritt 2. **In Arbeit — Batches `step-002` (Suppression, 8 Klassen, 7 Unit + 1 Integration) + `step-003` (Metrics, 7 Klassen, alle Unit) + `step-004` (Web/, 5 Klassen, alle Unit) + `step-005` (4 kleine Unit-Ordner Arch/Diag/FP/Cache gebündelt, 7 Klassen, alle Unit) + `step-006` (`Evals/`, 3 Klassen, alle Unit — `ListEvalsCommandTests`-Subprozess-Hypothese aus codemap.md step-002 in step-006 widerlegt: ruft `ListEvalsCommand.Run(console)` mit `TestLintConsole`-Mock aus `AiNetLinter.Tests.Output` direkt in-process auf, kein Subprozess) + `step-007` (`Output/` Teil 1/2, 5 Klassen alphabetisch D–O, alle Unit — `TestLintConsole.cs` Helper-Klasse ohne `[Fact]`/`[Theory]` ausgenommen, Heuristik-Punkt 6) + `step-008` (`Output/` Teil 2/2, restliche 4 Klassen alphabetisch P–V, alle Unit) + `step-009` (`Configuration/`, 8 Klassen, alle Unit, am 8-Item-Deckel = 1 Batch) done (approved, Commits `b484627`/`b4a8c59` — Heuristik-Punkt 7 als TD-005 BOM-Inhomogenität vom Kritiker eleviert); `step-010` (`Core/Checkers/` Teil 1/3, 8 Klassen alphabetisch A–`MethodParameterCountAccessibility`, alle Unit) done (approved, Commit `44956b7`); `step-011` (`Core/Checkers/` Rest 12 Klassen `MethodParameterCountIgnoreTypePrefixes`–`WpfCodeBehind` + 8 erste `Core/`-Klassen alphabetisch `AutoFixerTests`–`LinterEngineCacheTests`, **20 Klassen total**, alle Unit, **Mega-Batch** durch gelockerten `max_batch_items: 20` per `config.md` vom 2026-08-08) done (approved, Commits `bb39619`+`daad777` — Filter-Delta exakt Plankonform: Unit 706→882, Integration 113±0, Total 1325±0; **Pipeline-Konvention etabliert:** Code-Commit zuerst, Hash direkt in `step-result.md`, Doku-Commit danach — vermeidet den 3-Commits-Mechanismus aus step-011; **Kritiker-Hinweis:** Subject-Länge 77 Zeichen im Korrektur-Commit `daad777` bleibt als TD-002-MINOR dokumentiert, Folge-Steps halten 72-Grenze von vornherein ein); **`step-012` (`Core/` Rest 11 Klassen `LinterEngineTests`–`ViolationDescriptionTests` + `Maps/`+`Maps/Skeleton/` 6 Klassen, **17 Klassen total**, alle Unit, Mega-Batch) done (approved, Commits `b2477f5`/`7deeff1` — Filter-Delta exakt: Unit 882→**984** (+102), Integration 113±0, Total 1325±0; `Core/` damit **19/19**, `Maps/` **6/6** abgehakt; MINOR: CodeMap-Update fehlte im Coder-Doku-Commit → Planer step-013 nachgezogen; TD-007 Hilfsdateien-Leichen `auto_fixable: ja`); **`step-013` (in Planung, Mega-Batch):** gesamter Ordner `Mcp/Tools/` (**17 Klassen**, alle homogen Unit — 15× `IClassFixture<SymbolGraphCatalogFixture>` / dual `BaselineCatalogFixture`, 1× Mini-Fixture `ReloadConfigToolTests`, 0 Subprozess-Marker) + TD-007-Löschung = **18 Items** ≤ 20-Item-Deckel; erwartetes Filter-Delta Unit 984→**1130** (+146), Integration 113±0, Total 1325±0; 2 LF-only-Dateien (`GetServerHealthToolTests`, `ReloadConfigToolTests`) → Python-Helper. **Rest EPIC-02 danach:** `Mcp/`-Root (5 ungetaggte Testklassen + bereits getaggte Integration/Unit-Mix), `Baseline/` (gemischt), `Commands/` (stark gemischt), `Cli/` (Rest).**** `step-013` done (approved, Commits `0d5cee2`/`5c4600c` — Filter-Delta exakt: Unit 984→**1130** (+146), Integration 113±0, Total 1325±0; `Mcp/Tools/` damit **17/17** abgehakt); **`step-014` (in Planung, Mega-Batch, 20 Items = exakt am `max_batch_items: 20`-Deckel):** kompletter, durch eigene Code-Lese-Verifikation (nicht nur Prognose) bestätigter Rest-Bestand aus `Mcp/`-Root (5 Klassen, alle Unit), `Baseline/` (8 Klassen: 7 Unit + 1 Integration `WebBaselineTests`), `Commands/` (5 einfache Klassen: 1 Unit `ListRulesCommandTests` + 4 Integration; **explizit ausgeklammert:** `McpServerCommandTests` — 23 Fact-Methoden, davon nur 3 bereits Unit-getaggt, die restlichen 20 sind methodenweise gemischt 9 Unit/11 Integration, qualitativ eigener Sorgfaltsaufwand, eigener Folge-Step nach step-014), `Cli/` (2 Klassen, beide Unit trotz irreführender Namen `IgnoreSuppressionsCliTests`/`IgnoreSuppressionsIntegrationTests`); **Ist-Zustands-Korrektur gegenüber der step-013-Prognose:** `SourceFileCatalogRegisterMSBuildTests` war entgegen der alten Notiz bereits vollständig method-level Unit getaggt (kein Handlungsbedarf); erwartetes Filter-Delta Unit 1130→**1184** (+54), Integration 113→**121** (+8), Total 1325±0. **Letzter EPIC-02-Schritt danach (nur informativ):** `McpServerCommandTests.cs` allein (20 method-level Trait-Entscheidungen in einer Klasse) — danach ist EPIC-02 vollständig abgeschlossen. `Output/`-Schnitt-Begründung: Ordner enthält 10 `.cs`-Dateien, davon 9 Testklassen + 1 Helper (`TestLintConsole.cs` = `internal sealed class TestLintConsole : ILintConsole`, ohne `[Fact]`/`[Theory]`, wird aus dem Tagging-Scope ausgenommen — Heuristik-Punkt 6, neu in step-007, in step-008 ohne Ausnahme bestätigt = vollständig abgehakt). 9 > 8-Item-Deckel → alphabetisch in 2 Batches geschnitten: **step-007 (done)** = `DebtReportBuilderHeaderTests`, `DebtReportBuilderTests`, `LinterErrorFormatterTests`, `McpLintConsoleTests`, `OutputRootResolverTests` (5 Klassen, +13 Unit-Delta: 3+1+6+3+3 = 16 `[Fact]`, davon 3 in `McpLintConsoleTests` bereits method-level `Unit` getaggt → 13 neu für den Unit-Filter; EOL-Inhomogenität in `McpLintConsoleTests.cs` als TD-003 dokumentiert, low-prio Konsolidierungs-Vorschlag `git add --renormalize .`); **step-008 (geplant)** = `PathNormalizerTests`, `RuleLegendRegistryTests`, `ViolationMarkdownFormatterTests` (Heavyweight: 30 `[Fact]`, 473 Zeilen), `ViolationSummaryBuilderTests` (4 Klassen; **Test-Case-Anzahl wegen `[Theory]+[MemberData]` in `RuleLegendRegistryTests` von `RuleMetadataRegistry.KnownRuleNames` abhängig — vom step-008-Planer im Schritt 2 verifiziert: `KnownRuleNames.Count = 59`** aus 4 Partial-Dateien `RuleRegistry.cs` (3+4+2+6+3 = 18) + `RuleRegistry.Architecture.cs` (3+1 = 4) + `RuleRegistry.General.cs` (7+6+6+3 = 22) + `RuleRegistry.Web.cs` (15, transitiv via `BuildGeneralRules→BuildWebAssetRules`) = 18+4+22+15 = 59 transitive RuleMetadata-Literale, alle mit `Warum: ...` ≠ null/empty; **erwartetes Filter-Delta step-008**: 8+179+30+4 = +221 Test-Cases für Unit, **Unit 368 → 589**, Integration **113** unverändert, Total **1325** unverändert; regex-basierte Methoden-Zählung pro Datei: PathNormalizerTests=4 Methoden, RuleLegendRegistryTests=5 Methoden, ViolationMarkdownFormatterTests=30 Methoden, ViolationSummaryBuilderTests=4 Methoden = 43 Methoden vs. 221 Test-Cases zur Laufzeit — **Diskrepanz kommt von `[Theory]+[MemberData]`-Expansion in xUnit**). Stand nach step-007: 35 Klassen / 481 getaggte Tests (368 Unit + 113 Integration) von 1325 Gesamt, 844 ungetaggte Methoden, Rest-Bestand ~129 Klassen / 9+ Batches geschätzt. Erwartet nach step-008: 39 Klassen / 702 getaggte Tests (589 Unit + 113 Integration), 623 ungetaggte Methoden, Rest-Bestand ca. 125 Klassen / 9+ Batches geschätzt (vorher ~133 Klassen / 10+ Batches; konsolidiert sich weiter runter mit jedem Batch). **Tatsächlich nach step-008** (Review-approved, siehe `step-008/step-result.md`): **589 Unit / 113 Integration / 1325 Total** — exakt deckungsgleich mit der Erwartung. **Tatsächlich nach step-009** (Review-approved, siehe `step-009/step-result.md`): **656 Unit / 113 Integration / 1325 Total** — Filter-Delta **+67** statt der ursprünglich prognostizierten +68, weil der 16. `[Fact]`-Marker in `AgentFeaturesTests.cs` Z. 241 innerhalb eines `const string testClass = """ ... """`-Raw-String-Literals sitzt und kein echtes xUnit-Attribut ist (vom xUnit-Runner korrekt ignoriert, Planer-Miscount in step-009 transparent dokumentiert; step-010-Planer berücksichtigt diese String-Literal-`[Fact]`-NITPICK-Linie aus step-009-Review explizit). Erwartet nach step-009-Plan (offiziell): 657 Unit (—1 Abweichung String-Literal-`[Fact]`, dokumentiert in `step-009/step-result.md` §"Abweichungen"). **Erwartet nach step-010** (geplant, `Core/Checkers/` Teil 1/3, 8 Klassen A–MethodParameterCountAccessibility, alle homogen Unit ohne Subprozess-Marker, 50 `[Fact]` + 0 `[Theory]` = +50 Test-Cases alle Unit): **706 Unit / 113 Integration / 1325 Total** (Filter-Delta **+50**). Configuration/-Schnitt-Begründung (durch step-009 done — abgehakt): 8 Klassen homogen Unit (0 Subprozess-Marker, 0 `IClassFixture`, 0 bestehende Traits, alle ohne `using Microsoft.CodeAnalysis`-Subprozess-Bootstrap, regex-verifiziert per `grep -cE 'Process\.Start|McpTestClient|CliProcessRunner|Program\.Main|IClassFixture'` = 0/0/0/0/0/0/0/0) — passten genau in den 8-Item-Deckel (`max_batch_items: 8` aus `task-state.md` Config) = 1 Ordner = 1 Batch, keine Aufteilung nötig, alphabetische Sortierung nicht relevant (alle homogen). `Core/Checkers/`-Schnitt-Begründung: 27 Klassen im Ordner, **davon 7 bereits mit `[Trait("Category", "Unit")]` auf Klassen-Ebene getaggt** (NICHT durch EPIC-02-Schritte, sondern durch Refactoring-Commits `8cae25c refactor(tests): Core/-Testordner sub-gliedern ...` und `d744dc9 refactor(tests): Config-Konstruktion Rest-Cluster auf TestHelper konsolidiert ...` aus dem `[codegraph-mcp-finish]`-Feature-Branch, vermutlich beim damaligen Refactoring mitgenommen). Die 7 vorab-getaggten Klassen: `MaxInheritanceDepthTests`, `MaxConstructorDependenciesTests`, `MaxBoolParameterCountTests`, `MaxPublicMembersPerTypeTests`, `MaxSwitchArmsTests`, `NamespaceDirectoryMappingTests`, `NestedTypesCheckerTests` (verifiziert per `grep -nE '\[Trait\('` über alle 27 Dateien). **Verbleibend 20 ungetaggte Klassen**, alphabetisch geschnitten in **3 Batches** (8+8+4 = 20): **step-010 (done, approved, Commit `44956b7`)** = 8 Klassen A–MethodParameterCountAccessibility (`AsciiIdentifiersTests` 6 `[Fact]`, `AsyncVoidCheckerTests` 8 `[Fact]`, `BlockingTaskCheckerTests` 8 `[Fact]`, `CouplingSemanticTests` 2 `[Fact]`, `DynamicTypeCheckerTests` 1 `[Fact]`, `LinqChainLengthCheckerTests` 7 `[Fact]`, `MaxPartialClassFilesTests` 7 `[Fact]`, `MethodParameterCountAccessibilityTests` 11 `[Fact]`; 6+8+8+2+1+7+7+11 = 50 `[Fact]`, +50 Unit-Delta; **erwartet** 706 Unit / 113 Integration / 1325 Total); **step-011 (in Planung, Mega-Batch per `config.md` 2026-08-08 mit `max_batch_items: 20`)** = 20 Klassen = 12 verbleibende `Core/Checkers/`-Klassen M–W (vereint die ursprünglich im 8-Item-Cap-Plan vorgesehenen 8+4 = step-011+step-012) + 8 erste `Core/`-Klassen alphabetisch A–`LinterEngineCacheTests`; **step-012 (geplant, nach step-011)** = 11 verbleibende `Core/`-Klassen `LinterEngineTests`–`ViolationDescriptionTests` (1 Batch, da 11 ≤ 20-Item-Cap). **Warum 8+8+4 statt 8+8+8+3 (Orchestrator-Vorschlag):** die Orchestrator-Annahme basierte auf 27 ungetaggten Klassen; nach Abzug der 7 vorab-getaggten sind es real 20 → 8+8+4 = 3 Batches, **kein** 4. Batch. Die 8 step-010-Klassen sind alle BOM-/EOL-/Nullable-uniform über den Ordner (5/8 mit BOM, 8/8 uniform CRLF, 8/8 mit `#nullable enable`), alle Standard-Insert-Variante (kein `// @covers`, kein XML-Doc, kein `: IDisposable`) — keine neue Trait-Platzierungs-Variante gegenüber step-007/008/009-Bibliothek nötig. Heuristik-Punkt 7 (`Configuration/`-BOM-Inhomogenität — **durch step-009 done → TD-005 abgehoben**, s. `tech-debt.md`). Heuristik-Punkt 8 (**neu in step-010 beobachtet, dokumentiert, nicht angewendet**): **BOM-Inhomogenität** in `Core/Checkers/` — 10 von 27 Dateien mit UTF-8-BOM (37 %); 17 ohne BOM (63 %). Konkret mit BOM: `AsyncVoidCheckerTests`, `BlockingTaskCheckerTests`, `CouplingSemanticTests`, `LinqChainLengthCheckerTests`, `MethodParameterCountAccessibilityTests`, `MethodParameterCountIgnoreTypePrefixesTests`, `MethodParameterCountOverrideTests`, `SilentCatchAllowedTypesTests`, `SwitchDispatcherDetectorTests`, `WpfCodeBehindTests` (10/27). 5/8 step-010-Dateien mit BOM (alle BOM-tragenden der ersten 5 Klassen alphabetisch). **3 verschiedene Ordner, 3 verschiedene BOM-Verteilungen** beobachtet: `Output/` 0/9 = 0 %, `Core/Checkers/` 10/27 = 37 %, `Configuration/` 4/8 = 50 % — eine dritte Beobachtung, die die TD-005-Wurzel-Hypothese (gemeinsames `core.autocrlf` + Editor-Default-Verhalten) stützt, aber **kein step-010-Scope** zur Konsolidierung. Konservierung pro Datei **mechanisch** (Standard-Edit-Tool erhält BOM, Byte-Scan vorher/nachher verifiziert analog step-009 Vorgehen), Konsolidierung out of scope step-010 (kein `auto_fixable`-Anhängen, kein TD-Eintrag durch Planer — Beobachtung für Kritiker-Eskalation möglich, analog TD-005-Elevation in step-009-Review). Pattern-Beobachtung nun **dritter beobachteter Inhomogenitäts-Cluster** (TD-003 EOL in `Output/`, TD-004 Nullable in `Output/`, TD-005 BOM in `Configuration/`, neue BOM-Beobachtung in `Core/Checkers/`) — wahrscheinlich gemeinsame Wurzel im `core.autocrlf`/Editor-Encoding-Default, aber **kein** Planer-Scope zur Klärung.

- [ ] EPIC-03: Fixture-Sharing im großen Stil umsetzen — geleitet vom Spike aus EPIC-01, die identifizierten Testklassen auf `ICollectionFixture` (mit expliziten `[CollectionDefinition]`-Klassen) umstellen; Reihenfolge so, dass bestehende `parallelizeTestCollections: true` erhalten bleibt; Tests, die Fixture-State mutieren, ggf. durch Reset-Hooks oder eigene (nicht-geteilte) Fixtures ausnehmen. Konzept §"Wie" Schritt 3.

- [ ] EPIC-04: Fast-Path-Befehl etablieren + Doku — dokumentierten/verifizierten Befehl für den Alltag schaffen (z. B. PowerShell-Helferlein oder AGENTS.md-Eintrag), der die `Unit`-Kategorie filtert; sicherstellen, dass Volllauf und Fast-Path beide grün sind; Verifikation mit 3+ Läufen pro Variante. Konzept §"Wie" Schritt 4, §"Definition of Done" Punkt "dokumentierter Fast-Path-Befehl".

- [ ] EPIC-05: Produktionscode — leichterer/mockbarer Lade-Pfad (bedingt / Nice-to-Have) — nur falls EPIC-01 zeigt, dass Fixture-Sharing allein nicht ausreicht: prüfen, ob `SourceFileCatalog` / `McpCodeGraphServer` einen in-process-Lade-Pfad bekommen können, damit Tests, die aktuell nur wegen Testbarkeit einen echten `.exe`-Subprozess starten, ohne diesen auskommen. Sichtbares CLI/MCP-Verhalten darf sich nicht ändern. Konzept §"Wie" Schritt 5, §"Muss-Haven" letzter Punkt; Nice-to-Have-Charakter explizit aus Konzept-Scope.

- [ ] EPIC-06: Flaky-Test strukturell fixen — `McpServerCommandLoadingStateTests.LoadState_LoadFuncCompletesSynchronouslyWithCatalog_ReportsLoadedImmediately` (Test-Datei Zeile ~112-150) vom Poll-Loop mit fixer 5s-Deadline auf `TaskCompletionSource`/Event-basiertes Warten auf den `LoadState`-Übergang umstellen; Thread-Pool-Timing-Abhängigkeit eliminieren. Verifikation: 10 aufeinanderfolgende **volle** Testläufe grün (nicht isoliert). Konzept §"Wie" Schritt 6, §"Muss-Haven" Flaky-Punkt, §"Definition of Done" Flaky-Kriterium.

- [ ] EPIC-07: Tote `ConsoleTestCollection`-Infrastruktur entfernen (Nice-to-Have) — falls im Zuge von EPIC-03 ohnehin angefasst, dort mit erledigen; sonst eigenständiger Kleinst-Schritt. Nicht erzwingen — laut Konzept §"Nice-to-Have" optional. Konzept §"Wie" Schritt 7, §"Entdeckte Mängel" (tote Serialisierungs-Infrastruktur).

- [ ] EPIC-08: Abschluss-Validierung & Vorher/Nachher-Doku — vollen Testlauf mit optimiertem Setup mehrfach laufen lassen, Median bilden und mit der ~90s-Baseline aus Konzept vergleichen; DoD-Punkte aus Konzept §"Definition of Done" vollständig durchgehen und im Task-Ergebnis dokumentieren (kein festes Zeitziel, nur "spürbar besser"); Self-Lint grün. Konzept §"Definition of Done".

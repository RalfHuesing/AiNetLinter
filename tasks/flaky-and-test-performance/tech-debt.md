---
task: flaky-and-test-performance
type: tech-debt-log
maintained_by: kritiker
last_updated: 2026-08-07T18:22:00+02:00
---

# Tech-Debt-Log: flaky-and-test-performance

Append-only für offene Einträge. Jeder Eintrag ist eine vom Kritiker während eines
Step-Reviews beobachtete, aber bewusst **nicht** gefixte Auffälligkeit
außerhalb des Scopes des jeweiligen Steps (Architektur, Anti-Pattern,
Duplikation, Konsistenz) — siehe `../spec.md` §8.3/§9.

**Priorität ist reine Sortierhilfe für den Menschen, kein Auslöser.**

## Index

| ID | Bereich / Datei | Priorität | Kurzfassung |
|---|---|---|---|
| TD-002 | `tasks/.../step-*` + `.agents/.../coder/SKILL.md` §Schritt-5 | niedrig | Subject-Längen-/Typ-Disziplin: 72-Grenze und irreführende `test:`-Subjects für Doku-Commits (u. a. step-012 `7deeff1`). |
| TD-005 | `src/AiNetLinter.Tests/Configuration/` (4 von 8 Test-Dateien) | niedrig | UTF-8-BOM-Inhomogenität in `Configuration/`: 4 von 8 `.cs`-Dateien mit BOM, 4 ohne — Repository-weite Konsistenz-Frage ohne funktionale Auswirkung. |
| TD-006 | `Core/Checkers/` + `Core/` (BOM) | niedrig | UTF-8-BOM-Inhomogenität: `Core/Checkers/` 10/27 + `Core/`-Rest step-012 4/11 mit BOM; byte-genau konserviert. |
| TD-008 | `src/AiNetLinter/Mcp/AnalysisToolRegistrations.cs` (`rules.json`-`PathOverride`) | mittel | `AIContextFootprint`-Limit (2870) hat bereits vor step-019 null Headroom — jede weitere Zeile in `McpCodeGraphServer.cs` o.ä. reißt es sofort. |
| TD-009 | `src/AiNetLinter.Tests/Fixtures/*` (Subprozess-startende Fixtures, Windows) | niedrig | Verwaiste `AiNetLinter.exe`-Subprozesse sterben nicht mit dem Eltern-Testprozess (kein Process-Tree-/Job-Object-Kill) — kann Datei-Locks/Folgefehler nach abnormalem Testlauf-Abbruch verursachen. |
| TD-010 | `McpServerCommandErrorHandlingTests`, `McpServerCommandJsonRpcFramingTests` u. a. MCP-Integrationstests | hoch | Im vollen `dotnet test`-Lauf unter Last reproduzierbar (3× in step-019 beobachtet) MSBuildWorkspace-/Subprozess-bedingte Timeouts bzw. Hänger, unabhängig von step-019 — Risiko für EPIC-08-Abschluss-Validierung. |

## Einträge

### TD-002 — Subject-Längen-Disziplin bei Code-/Doku-Commits (Skills/Plan-Genauigkeit) [Priorität: niedrig]

- **Gefunden in:** step-003 (Kritiker-Review vom 2026-08-07T10:35:00+02:00), Muster aus step-001 (94-Zeichen-Review-Commit `71ab96b`) und step-002 (74-Zeichen-Doku-Commit `79d3d6d`) bestätigt die Wiederholungs-Charakteristik.
- **Ort:** `tasks/flaky-and-test-performance/step-003/step-plan.md:259-260` (Plan-DoD mit falscher Längenangabe "71 Zeichen"), `tasks/flaky-and-test-performance/step-003/step-result.md:53` (Doku-Commit-Subject 91 Zeichen), `67fb86b` (Code-Commit-Subject 85 Zeichen); `skills/coder/SKILL.md:97` ("Subject ≤ 72 Zeichen inkl. Suffix"), `spec.md` §10.3 Z. 481-482 (Suffix zählt gegen 72-Grenze), `spec.md` §10.7 (History-Reset absolut verboten).
- **Befund:** Die 72-Zeichen-Grenze für Commit-Subjects (siehe `skills/coder/SKILL.md` §Schritt-5 + `spec.md` §10.3) wird in diesem Task mehrfach überschritten — bislang ohne Code-/Test-Impact, aber als wiederkehrende Stil-Abweichung. Konkrete Schritte:
  - `step-001` Review-Commit `71ab96b` (`docs(task): step-001 Review dokumentieren (Verdict: approved) [flaky-and-test-performance] …`): 490 Zeichen **bzw. nur-Subject 94 Zeichen** (Body ohne Blankline an Subject angeflanscht — separates Format-Issue, siehe unten) — **Subject 94 Zeichen, 22 über Grenze**, approved.
  - `step-002` Doku-Commit `79d3d6d` (`docs(tasks): step-002 Result dokumentieren [flaky-and-test-performance]`): 71 Zeichen, **knapp unter Grenze** — approved.
  - `step-003` Code-Commit `67fb86b` (`chore(tests): Metrics-Tests mit Category-Traits versehen [flaky-and-test-performance]`): **85 Zeichen, 13 über Grenze** — Plan-DoD hatte fälschlich "71 Zeichen, unter 72" vorgegeben; Coder hat die Abweichung dokumentiert, aber **nicht** amendet (per `spec.md` §10.7 verboten).
  - `step-003` Doku-Commit `03b04f4` (`docs(tasks): step-003 Result und Status 'done (pending audit)' [flaky-and-test-performance]`): **91 Zeichen, 19 über Grenze** — approved mit MINOR, da Coder keine Korrektur-Möglichkeit hat.
  - **Zusatzbefund (sekundär):** Mehrere step-001/step-002/step-003-Planungs- und Review-Commits haben Subject + Body **ohne Leerzeile** als einen einzigen Long-Line committet (z. B. `4e3044d` = 413 Zeichen in `%s`, `1aafb19` = 519 Zeichen) — `git log --oneline` zeigt dann den ganzen Text inkl. Bullet-Liste; das ist ein separates Commit-Format-Issue (fehlende Trennung Subject/Body) und wird hier nur als Beobachtung vermerkt.
- **Warum nicht sofort gefixt:** Außerhalb des Scopes von step-003 (rein additives Attribut auf Klassen-Ebene, Subject-Länge ist eine Commit-Message-Frage). Eine nachträgliche Korrektur der bereits committeten Subjects ist per `spec.md` §10.7 absolut verboten (`git commit --amend`, `git rebase`, `git reset --hard/--soft` auf bereits committete Commits, `git filter-branch`/`filter-repo`, Force-Push — alle ausnahmslos verboten). Die Korrektur kann erst beim nächsten Code-Commit desselben Tasks oder in einem separaten Rule-Update-Step erfolgen.
- **Vorschlag (zwei alternative Richtungen — Nutzer-/Orchestrator-Entscheidung):**
  - **(a) Planer-Disziplin + Skill-Präzisierung:** Planer gibt in `step-plan.md` DoD einen **konkreten Subject-String** mit korrekter Längenangabe vor (z. B. `chore(tests): Metrics-Traits [flaky-and-test-performance]` = 56 Zeichen) und verzichtet auf Body-Daten, die der Coder selbst gut formulieren kann. `skills/coder/SKILL.md` §Schritt-5 könnte um die explizite Empfehlung "bei absehbarer Subject-Länge >60 Zeichen, im Plan-DoD alternative kürzere Subject-Vorschläge auflisten" ergänzt werden. Coder akzeptiert den Subject-Vorschlag, ggf. mit leichter Anpassung.
  - **(b) Regel-Lockerung für Doku-Commits:** `AiNetLinterRichtlinien.mdc` §4 könnte um eine explizite Ausnahme "Für `docs(...)`- und `chore(task)`-Commits gilt eine gelockerte Obergrenze von 100 Zeichen, da der Subject hier primär Doku-/Audit-Funktion hat und keine Code-Änderung beschreibt" ergänzt werden. Vorteil: pragmatisch, kein zusätzlicher Planer-Aufwand, keine künstlich verkürzten Subject-Strings. Nachteil: Regel-Ausnahme → schwerer zu merken, könnte für Nicht-Doku-Commits als Präzedenz missbraucht werden.
  - **Empfehlung:** Variante (a) — sie ist Spec-treu, ändert die Regel nicht und verlagert die Disziplin in den Planer-Aufruf, wo sie hingehört (Längenvorgabe ist Planer-Wissen, nicht Coder-Wissen).
- **Erweiterung step-012 (Kritiker-Review 2026-08-07T15:35:00+02:00):** Doku-Commit `7deeff1` verwendet **identisches Subject** wie Code-Commit `b2477f5` (`test: Core+Maps-Tests Kategorie-taggen [flaky-and-test-performance]`, 67 Zeichen — Länge ok, aber Conventional-Commit-Typ `test:` für rein dokumentierende Änderungen inkl. Hilfsdateien). Plan-DoD hatte `docs(tasks): step-012 Result dokumentieren […]` (66 Zeichen) als Beispiel empfohlen. Kein neuer TD-Eintrag — gleicher Mechanismus.
- **Status:** offen

### TD-005 — UTF-8-BOM-Inhomogenität in `src/AiNetLinter.Tests/Configuration/` (4 von 8 mit BOM, 4 ohne) [Priorität: niedrig]

- **Gefunden in:** step-009 (Kritiker-Review vom 2026-08-07T14:02:00+02:00), vom Coder in `step-009/step-result.md` §"Beobachtungen" als Heuristik-Punkt 7 (in spe, neu beobachtet) bereits benannt, vom Planer in `step-009/step-plan.md` §"Aktueller Projektzustand" / §"Notes" ausdrücklich als „nicht TD-Eintrag angelegt (Beobachtung ohne Konsolidierungs-Auftrag)" markiert — der Kritiker elevated die Beobachtung wegen struktureller Analogie zu TD-003 (EOL-Inhomogenität in `Output/`) und TD-004 (Nullable-Inhomogenität in `Output/`).
- **Ort:** `src/AiNetLinter.Tests/Configuration/`-Ordner, 4 von 8 `.cs`-Dateien mit UTF-8-BOM (`EF BB BF` als erste 3 Bytes), 4 ohne (erste 3 Bytes = `using` oder `#nu`):
  - **MIT BOM:** `ConfigLoaderRulesJsonTests.cs`, `ConfigNormalizerTests.cs`, `ConfigSyncerTests.cs`, `PathOverridesTests.cs` (4/8 = 50 %)
  - **OHNE BOM:** `AgentFeaturesTests.cs`, `DeveloperExperienceTests.cs`, `FileFilterEvaluatorTests.cs`, `RuleMetadataRegistryTests.cs` (4/8 = 50 %)
  - Verifiziert per `[System.IO.File]::ReadAllBytes(...)` über alle 8 Dateien (erste 3 Bytes-Vergleich).
- **Befund:** Im `Configuration/`-Ordner ist die UTF-8-BOM-Verteilung 4/8 mit und 4/8 ohne BOM — eine **neue Inhomogenitäts-Dimension** (Encoding/BOM), die in `Output/` (TD-003 EOL und TD-004 Nullable sind dort die beobachteten Dimensionen) so nicht existiert. Die BOM-Teilmenge ist **nicht** deckungsgleich mit der `#nullable enable`-Teilmenge (Configuration/ hat 5/8 **mit** Direktive, 3/8 **ohne** — umgekehrte Mehrheit als Output/, und eine **andere** Datei-Teilmenge als die BOM-Teilmenge) und auch nicht mit EOL (alle 8 Configuration/-Dateien uniform CRLF, also TD-003 dort nicht reproduziert). Es handelt sich also um **3 unabhängige Inhomogenitäts-Dimensionen** (BOM, EOL, Nullable) mit gemeinsamer wahrscheinlicher Wurzel (Repository-/Editor-/Checkout-Spezialverhalten, vermutlich `core.autocrlf` + Encoding-Defaults). **Funktional** wirkt sich die fehlende oder vorhandene BOM nicht aus: `dotnet build` (TreatWarningsAsErrors) ist grün, alle 1325/1325 Tests grün, Self-Lint `OK`. Inkonsistenz jedoch sichtbar in jedem `git status` über die 4 BOM-tragenden Dateien und in jedem `git diff` (z. B. `LF will be replaced by CRLF`-Hinweis beim Checkout, sofern Git die BOM als Encoding-Marker bewertet).
- **Warum nicht sofort gefixt:** Außerhalb des Scopes von step-009 (rein additives Attribut auf Klassen-Ebene, kein BOM-Konsistenz-Auftrag). Die Frage „CRLF beibehalten wie 9 Schwestern oder LF als moderner Standard etablieren" (TD-003-Analogie hier: „BOM in allen 8 Dateien oder in keiner") ist eine **Nutzer-/Repo-Konvention-Entscheidung**, nicht rein mechanisch. Eine Renormalisierung auf „alle 8 mit BOM" oder „alle 8 ohne BOM" setzt den `core.autocrlf`-Default des Repos bzw. eine `.gitattributes`-Regel fort; die Entscheidung ist mit TD-003/TD-004 symmetrisch.
- **Vorschlag (zwei alternative Richtungen — Nutzer-/Orchestrator-Entscheidung):**
  - **(a) Lokale Renormalisierung Configuration/-BOM vereinheitlichen:** `git add --renormalize .` über `src/AiNetLinter.Tests/Configuration/` (oder nur die 4 abweichenden Dateien) + Commit mit Subject wie `chore(tests): Configuration/-BOM normalisieren [flaky-and-test-performance]`. Greift die Dateien auf den Ordner-Standard zurück, kein Verhaltens-Effekt. Erfordert vorher die Entscheidung „mit oder ohne BOM" (z. B. „alle 8 mit BOM, weil 4/8 schon so sind" = default-konservativ). Erfordert keinen weiteren Mechanik-Schritt; passt als `auto_fixable: ja`-Bündel in einen ohnehin laufenden EPIC-02-Folge-Step (analog TD-003-Variante-a-Empfehlung).
  - **(b) Repo-weite `.gitattributes`-Regel:** `.gitattributes` mit `*.cs text=auto eol=crlf` (oder `eol=lf`) + projektweite Konsolidierung. Konsistent mit modernen Cross-Plattform-Repos, entfernt die Windows-only-Inhomogenität global. Hat aber Auswirkungen auf den gesamten `Configuration/`-Ordner, viele weitere Dateien in `Output/`, `Evals/` etc. — eigenständiger Step mit Vorab-Bestandsaufnahme sinnvoll.
  - **(c) Status quo belassen, Beobachtung dokumentiert:** wenn der Nutzer die Inhomogenität toleriert (z. B. weil jeder Editor die BOM korrekt verarbeitet und das Team keine Probleme hat), bleibt TD-005 als offene Beobachtung stehen und beeinflusst keine Folge-Steps. Aktuell favorisiert, weil in `Configuration/` (anders als in `Output/`) die BOM-Lage keine funktionalen, sondern rein stilistische Auswirkungen hat.
  - **Empfehlung:** Variante (a) zuerst, wenn überhaupt — minimal-invasiv, passt zur etablierten Konvention „1 Ordner = 1 Standard", kann als gebündeltes Aufräum-Item in einem EPIC-02-Folge-Step mitlaufen, **ohne** die Repo-Konvention global in Frage zu stellen. Variante (b) wäre ein eigenständiges, vom Nutzer angefordertes Cross-Plattform-Refactoring. Variante (c) ist die sichere Default-Entscheidung, falls der Nutzer keinen Konsolidierungs-Wunsch äußert.
- **auto_fixable:** nein
- **Status:** offen

### TD-006 — UTF-8-BOM-Inhomogenität in `src/AiNetLinter.Tests/Core/Checkers/` (10 von 27 mit BOM, 17 ohne) [Priorität: niedrig]

- **Gefunden in:** step-010 (Kritiker-Review vom 2026-08-07T14:30:00+02:00), vom Coder in `step-010/step-result.md` §"Beobachtungen" als Heuristik-Punkt 8 (in spe, neu beobachtet) bereits benannt, vom Planer in `step-010/step-plan.md` §"Aktueller Projektzustand" / §"Notes" ausdrücklich als „nicht TD-Eintrag angelegt (Konsolidierung out of scope)" markiert — der Kritiker elevated die Beobachtung wegen struktureller Analogie zu TD-003 (EOL-Inhomogenität in `Output/`), TD-004 (Nullable-Inhomogenität in `Output/`) und TD-005 (BOM-Inhomogenität in `Configuration/`).
- **Ort:** `src/AiNetLinter.Tests/Core/Checkers/`-Ordner, 10 von 27 `.cs`-Dateien mit UTF-8-BOM (`EF BB BF` als erste 3 Bytes), 17 ohne (erste 3 Bytes = `#nu` für `#nullable enable` oder `us` für `using`):
  - **MIT BOM (10/27 = 37 %):** `AsyncVoidCheckerTests.cs`, `BlockingTaskCheckerTests.cs`, `CouplingSemanticTests.cs`, `LinqChainLengthCheckerTests.cs`, `MethodParameterCountAccessibilityTests.cs` (5 in step-010 verifiziert); `MethodParameterCountIgnoreTypePrefixesTests.cs`, `MethodParameterCountOverrideTests.cs`, `SilentCatchAllowedTypesTests.cs`, `SwitchDispatcherDetectorTests.cs`, `WpfCodeBehindTests.cs` (5 weitere für step-011/012 vorgesehen).
  - **OHNE BOM (17/27 = 63 %):** `AsciiIdentifiersTests.cs`, `DynamicTypeCheckerTests.cs`, `MaxPartialClassFilesTests.cs` (3 in step-010 verifiziert); `MaxBoolParameterCountTests.cs`, `MaxConstructorDependenciesTests.cs`, `MaxInheritanceDepthTests.cs`, `MaxPublicMembersPerTypeTests.cs`, `MaxSwitchArmsTests.cs`, `MiddleManCheckerTests.cs`, `NamespaceCouplingCheckerTests.cs`, `NamespaceDirectoryMappingTests.cs`, `NamingCheckerTests.cs`, `NestedTypesCheckerTests.cs`, `PhantomDependencyCheckerTests.cs`, `SealedClassCheckerTests.cs`, `UiFileSeparationCheckerTests.cs`, `ValueObjectCheckerTests.cs` (14 weitere, davon 7 bereits getaggt aus `[codegraph-mcp-finish]`-Refactoring-Commits).
  - Verifiziert per `[System.IO.File]::ReadAllBytes(...)` über alle 27 Dateien (erste 3 Bytes-Vergleich).
- **Befund:** Im `Core/Checkers/`-Ordner ist die UTF-8-BOM-Verteilung 10/27 mit und 17/27 ohne BOM — eine **dritte Inhomogenitäts-Dimension** (Encoding/BOM), die in `Output/` (TD-003 EOL, TD-004 Nullable) und `Configuration/` (TD-005 BOM) so nicht existiert. Die BOM-Teilmenge ist **nicht** deckungsgleich mit der `#nullable enable`-Teilmenge (in `Core/Checkers/` haben alle 27 Dateien die Direktive, also TD-004 dort nicht reproduziert) und auch nicht mit EOL (alle 27 Dateien uniform CRLF, also TD-003 dort nicht reproduziert). Es handelt sich also um **3 unabhängige Inhomogenitäts-Dimensionen** (BOM, EOL, Nullable) mit gemeinsamer wahrscheinlicher Wurzel (Repository-/Editor-/Checkout-Spezialverhalten, vermutlich `core.autocrlf` + Encoding-Defaults). **Funktional** wirkt sich die fehlende oder vorhandene BOM nicht aus: `dotnet build` (TreatWarningsAsErrors) ist grün, alle 1325/1325 Tests grün, Self-Lint `OK`. Inkonsistenz jedoch sichtbar in jedem `git status` über die 10 BOM-tragenden Dateien und in jedem `git diff` (z. B. `LF will be replaced by CRLF`-Hinweis beim Checkout, sofern Git die BOM als Encoding-Marker bewertet).
- **Warum nicht sofort gefixt:** Außerhalb des Scopes von step-010 (rein additives Attribut auf Klassen-Ebene, kein BOM-Konsistenz-Auftrag). Die Frage „BOM in allen 27 Dateien oder in keiner" ist eine **Nutzer-/Repo-Konvention-Entscheidung**, nicht rein mechanisch. Eine Renormalisierung auf „alle 27 mit BOM" oder „alle 27 ohne BOM" setzt den `core.autocrlf`-Default des Repos bzw. eine `.gitattributes`-Regel fort; die Entscheidung ist mit TD-003/TD-004/TD-005 symmetrisch.
- **Vorschlag (zwei alternative Richtungen — Nutzer-/Orchestrator-Entscheidung):**
  - **(a) Lokale Renormalisierung `Core/Checkers/`-BOM vereinheitlichen:** `git add --renormalize .` über `src/AiNetLinter.Tests/Core/Checkers/` (oder nur die 17 abweichenden Dateien) + Commit mit Subject wie `chore(tests): Core/Checkers/-BOM normalisieren [flaky-and-test-performance]`. Greift die Dateien auf den Ordner-Standard zurück, kein Verhaltens-Effekt. Erfordert vorher die Entscheidung „mit oder ohne BOM" (z. B. „alle 27 mit BOM, weil 10/27 schon so sind" = default-konservativ, oder „alle 27 ohne BOM, weil 17/27 schon so sind" = default-majoritär). Erfordert keinen weiteren Mechanik-Schritt; passt als `auto_fixable: ja`-Bündel in einen ohnehin laufenden EPIC-02-Folge-Step (analog TD-005-Variante-a-Empfehlung).
  - **(b) Repo-weite `.gitattributes`-Regel:** `.gitattributes` mit `*.cs text=auto eol=crlf` (oder `eol=lf`) + projektweite Konsolidierung. Konsistent mit modernen Cross-Plattform-Repos, entfernt die Windows-only-Inhomogenität global. Hat aber Auswirkungen auf den gesamten `Core/Checkers/`-Ordner, viele weitere Dateien in `Output/`, `Configuration/`, `Evals/` etc. — eigenständiger Step mit Vorab-Bestandsaufnahme sinnvoll.
  - **(c) Status quo belassen, Beobachtung dokumentiert:** wenn der Nutzer die Inhomogenität toleriert (z. B. weil jeder Editor die BOM korrekt verarbeitet und das Team keine Probleme hat), bleibt TD-006 als offene Beobachtung stehen und beeinflusst keine Folge-Steps. Aktuell favorisiert, weil in `Core/Checkers/` (anders als in `Output/`) die BOM-Lage keine funktionalen, sondern rein stilistische Auswirkungen hat.
  - **Empfehlung:** Variante (a) zuerst, wenn überhaupt — minimal-invasiv, passt zur etablierten Konvention „1 Ordner = 1 Standard", kann als gebündeltes Aufräum-Item in einem EPIC-02-Folge-Step mitlaufen, **ohne** die Repo-Konvention global in Frage zu stellen. Variante (b) wäre ein eigenständiges, vom Nutzer angefordertes Cross-Plattform-Refactoring. Variante (c) ist die sichere Default-Entscheidung, falls der Nutzer keinen Konsolidierungs-Wunsch äußert.
- **Erweiterung step-012 (Kritiker-Review 2026-08-07T15:35:00+02:00):** BOM auch im `Core/`-Rest (nicht nur `Core/Checkers/`): 4/11 mit BOM (`NullCoalescingInitializerClassifierTests`, `ResultPatternNamespaceTests`, `ScopeImmutabilityTests`, `StaticTestSentinelExemptionTests`), Blob-Verify `EF BB BF` erhalten; `Maps/` 0/6 mit BOM. Kein neuer TD — Ort um `Core/`-Rest ergänzt.
- **auto_fixable:** nein
- **Status:** offen

### TD-008 — `AnalysisToolRegistrations.cs` ohne Headroom beim `AIContextFootprint`-Limit [Priorität: mittel]

- **Gefunden in:** step-019 (Kritiker-Review vom 2026-08-08T12:15:00+02:00), vom Coder in `step-019/step-result.md` §"Abweichungen vom Plan" + §"Beobachtungen" bereits per A/B-Test (`git stash`/`pop`) verifiziert und für den Kritiker vorgemerkt.
- **Ort:** `src/AiNetLinter/Mcp/AnalysisToolRegistrations.cs`; `rules.json` → `PathOverrides` → `"src/AiNetLinter/Mcp/AnalysisToolRegistrations.cs"` → `Metrics.MaxAIContextFootprint: 2870`. Selbst nachvollzogen (`dotnet run --project src/AiNetLinter -- --config rules.json --path src/AiNetLinter/Mcp/AnalysisToolRegistrations.cs` bricht mit unabhängigem Solution-Lade-Fehler ab, aber der Override-Wert 2870 ist im Regelwerk direkt einsehbar und deckt sich mit der Coder-A/B-Messung: mit 4-zeiligem XML-Doc-Kommentar auf der neuen `LoadTask`-Property 2875 (Verstoß), mit Ein-Zeiler 2871 (weiterhin Verstoß), ganz ohne Kommentar 2870 (`OK`, in diesem Review per vollem Self-Lint-Lauf bestätigt: `dotnet run --project src/AiNetLinter -- --config rules.json --path .` → `OK`).
- **Befund:** `AnalysisToolRegistrations.cs` (bzw. die transitiv darüber gemessenen Abhängigkeiten, u. a. `McpCodeGraphServer.cs`) steht exakt auf dem projektspezifischen `MaxAIContextFootprint`-Override von 2870 — ohne jeden Headroom. Jede künftige additive Änderung an `McpCodeGraphServer.cs` oder anderen in diese Metrik einfließenden Dateien (und sei es nur ein XML-Doc-Kommentar) reißt das Limit sofort, unabhängig davon, ob die Änderung inhaltlich sinnvoll wäre. step-019 selbst musste deshalb auf die eigentlich im Plan vorgesehene XML-Doc-Dokumentation der neuen `LoadTask`-Property verzichten.
- **Warum nicht sofort gefixt:** Außerhalb des Scopes von step-019 (EPIC-06 behebt ausschließlich den `LoadState`-Poll-Loop). Eine Anhebung des `rules.json`-Grenzwerts oder eine strukturelle Entlastung von `AnalysisToolRegistrations.cs` ist eine Architektur-/Konfigurationsentscheidung mit Ermessen (welcher neue Grenzwert ist angemessen? lohnt sich stattdessen eine Aufteilung der Registrierungsklasse?), keine mechanische Korrektur.
- **Vorschlag:** Vor dem nächsten Step, der `McpCodeGraphServer.cs` oder `AnalysisToolRegistrations.cs` selbst erweitert, entweder den Override moderat anheben (z. B. auf einen Wert mit spürbarem Headroom, analog zu den Nachbar-Overrides 2800-2900 in `rules.json`) oder `AnalysisToolRegistrations.cs` strukturell entlasten (z. B. Tool-Registrierungen aufteilen). Nutzer-/Planer-Entscheidung, kein Automatismus.
- **Auto-Fixable:** nein — Grenzwertwahl bzw. Strukturaufteilung ist Ermessenssache, keine entscheidungsfreie mechanische Korrektur.
- **Status:** offen

### TD-009 — Verwaiste `AiNetLinter.exe`-Subprozesse sterben unter Windows nicht mit dem Eltern-Testprozess [Priorität: niedrig]

- **Gefunden in:** step-019 (Kritiker-Review vom 2026-08-08T12:15:00+02:00), vom Coder in `step-019/step-result.md` §"Beobachtungen" benannt (in dieser Session mehrfach als scheinbarer "Hang" beobachtet, tatsächlich Kaskade aus vorangegangenen Prozess-Kills).
- **Ort:** `src/AiNetLinter.Tests/Fixtures/*` — alle Fixtures/Helper, die `AiNetLinter.exe` als Subprozess starten (u. a. `SymbolGraphMcpFixture.cs`, `McpLiveRepositoryFixture.cs`, `CliProcessRunner.cs`, `BaselineMcpFixture.cs`), betrifft die Prozessstart-Mechanik generell, keine einzelne Datei.
- **Befund:** Wird ein `dotnet test`-Lauf abnormal beendet (z. B. externes Prozess-Kill nach einem Hang), gibt es unter Windows/.NET aktuell keinen Process-Tree-/Job-Object-Kill, der gestartete `AiNetLinter.exe`-Kindprozesse zuverlässig mitbeendet. Solche Waisen können `bin\Debug\net10.0\*.dll` gesperrt lassen und nachfolgende `dotnet build`/`dotnet test`-Läufe mit `MSB3027`/`CS2012`-Kopier-/Schreibfehlern zum Scheitern bringen — was auf den ersten Blick wie ein neuer Hang aussieht, aber Folgeschaden eines früheren Abbruchs ist.
- **Warum nicht sofort gefixt:** Außerhalb des Scopes von step-019 (reine Testinfrastruktur-Robustheit, kein Bezug zum `LoadState`-Poll-Loop). Eine robuste Lösung (z. B. Windows-Job-Objects für alle per `Process.Start` gestarteten Subprozesse in den Test-Fixtures) ist eine strukturelle Änderung an mehreren Fixture-Dateien, kein Einzeiler.
- **Vorschlag:** Job-Object-basiertes oder Process-Tree-Kill-Cleanup (z. B. zentral in einer gemeinsamen Basis-Fixture/-Utility-Klasse) für alle subprozess-startenden Test-Fixtures einführen, damit ein abnormaler Testlauf-Abbruch keine Datei-Locks mehr hinterlässt.
- **Auto-Fixable:** nein — Architekturentscheidung (zentrale Utility vs. pro Fixture), Verhaltensänderung an mehreren Dateien.
- **Status:** offen

### TD-010 — Reproduzierbare MSBuildWorkspace-/Subprozess-Hänger bei anderen MCP-Integrationstests im vollen Testlauf unter Last [Priorität: hoch]

- **Gefunden in:** step-019 (Kritiker-Review vom 2026-08-08T12:15:00+02:00), vom Coder in `step-019/step-result.md` §"Beobachtungen" benannt und in der 10-Lauf-Kern-Verifikation selbst beobachtet (Run 9: 1 Fehlschlag, Run 10: 1 Hang), explizit auf Bitte des Orchestrators/Nutzers hier für den Kritiker festgehalten statt in step-019 behoben.
- **Ort:** `McpServerCommandErrorHandlingTests.RunAsync_ValidFixture_CompileErrorFileReturnsWarningSection` (Run 9: `TaskCanceledException` in `ModelContextProtocol.McpSessionHandler.SendRequestAsync`, MCP-Client-Timeout), `McpServerCommandJsonRpcFramingTests.Initialize_ResponseInstructionsField_ContainsServerInstructionsDoctrine` (Run 10: Hang, per Prozess-Kill beendet), in einem früheren Lauf zusätzlich `ToolCallSequence_AllStdoutLinesAreValidJsonRpcFrames` — alle drei unabhängig von `McpServerCommandLoadingStateTests` (step-019 berührt diese Testklassen nicht).
- **Befund:** Der volle `dotnet test`-Lauf zeigt unter Last (nicht isoliert/gefiltert reproduzierbar) reproduzierbare MSBuildWorkspace-/Subprozess-bedingte Timeouts bzw. Hänger bei mehreren, voneinander unabhängigen MCP-Integrationstests — 3× in der step-019-Session beobachtet über 3 verschiedene Testmethoden. Root Cause nicht step-019-spezifisch (keine Code-Pfad-Berührung durch die rein additive `LoadTask`-Property), aber strukturell demselben Grundproblem-Cluster zuzuordnen wie der ursprüngliche EPIC-06-Flaky-Test (Thread-Pool-/Subprozess-Kontention unter voller Testlast).
- **Warum nicht sofort gefixt:** Explizit out of scope für step-019 (EPIC-06 adressiert ausschließlich den `LoadState`-Poll-Loop, nicht alle MCP-Integrationstests). Root-Cause-Analyse für 3 unabhängige Testklassen wäre ein eigenständiger, nicht-trivialer Untersuchungsaufwand.
- **Vorschlag:** Vor bzw. im Rahmen von EPIC-08 (Abschluss-Validierung) gezielt prüfen, ob dieses Muster die dortige Vorher/Nachher-Verifikation gefährdet (EPIC-08 verlangt ebenfalls mehrfache volle Testläufe) — ggf. eigenes Epic für MCP-Client-Timeout-Härtung/Subprozess-Stabilität, falls der Nutzer das für nötig hält.
- **Auto-Fixable:** nein — Root Cause noch nicht lokalisiert, keine mechanische Korrektur möglich.
- **Status:** offen
